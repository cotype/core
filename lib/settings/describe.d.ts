import { Model } from "../../typings";
import { OpenApiBuilder } from "openapi3-ts";
/**
 * Adds all settings-related routes (defined in ./routes.ts) to an OpenAPI spec.
 */
export default function (api: OpenApiBuilder, model: Model): void;
