import { Model } from "../../typings";
import { Router } from "express";
import { Persistence } from "../persistence";
declare const _default: (router: Router, persistence: Persistence, model: Model) => void;
export default _default;
